<article id="user-show-<%= @user.id%>" class = "user_show">
  <h2><span>My Page Edit</span></h2>
    <%= form_with model: @user do |f| %>
      <section class="">
          <div class="icon sp-center text-center margin-auto">
            <%= f.label :avatar do %>
              <span class="filelabel" title="file select">
                <%= image_tag @user.avatar.url, class: 'avatar-icon' %>
                <span class="camera"></span>
              </span>
              <%= f.file_field :avatar, accept: 'image/*', style: 'display: none' %>
              <%= f.hidden_field :avatar_cashe %>
            <% end %>
          </div>
          <div class="body sp-center">
            <div class="profile-edit">
              <%= f.label :last_name %>
              <%= f.text_field :last_name %>
            </div>

            <div class="profile-edit">
              <%= f.label :first_name %>
              <%= f.text_field :first_name %>
            </div>
            <div class="profile-edit">
              <%= f.label :profile %>
              <%= f.text_area :profile %>
            </div>
          </div>
          <div class="block text-center margin-top-10">
            <%= f.submit 'update' %>
          </div>
      </section>
    <% end %>
</article>